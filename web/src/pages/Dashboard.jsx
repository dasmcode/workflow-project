import Layout from "../components/Layout";
import { Typography } from "@mui/material";

export default function Dashboard() {
  return (
    <Layout>
      <Typography variant="h5">Select a file or upload one</Typography>
    </Layout>
  );
}
